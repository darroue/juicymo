.btn-group.mb-3
  = new_link

.card
  .card-body
    .card-title
      h1
        = edit_label

    = render "form", project: @object

    .row.g-2.mt-1
      .col-12
        .btn-group
          = show_link(@object.id)
          = back_to_link

      .col-12
        = destroy_link(@object.id)
